var SetPositionCommand = function(object, x, y, z ) {
    //new changeBoundingSphereColorCommand()

    //viewPort.
    object.object3D.position.set( x, y, z );
    // object.object3D.traverse( function(child){

    //     child.visible = bool;

    // } )
    //console.log(config.color[cluster]);
}